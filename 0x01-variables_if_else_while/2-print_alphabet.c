#include <stdio.h>
#include <stdlib.h>

/* main - function prints alphabets in lower case
 * Return: ends the program
*/

int main(void)
{
	int x;
	for (x = 'a'; x <= 'z'; ++x)
		putchar("x");
	return (0);
}
