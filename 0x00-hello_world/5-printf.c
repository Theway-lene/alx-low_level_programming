#include <stdio.h>

/**
 * main - function to print text with a new line
 * nothing else, just pass out values
 * Return: value passed
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
